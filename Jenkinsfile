pipeline {
    agent any

    stages {
        stage('scm') {
            steps {
                // git branch: 'branch1', credentialsId: 'github-cred', url: 'https://github.com/surya22ganesh/docker.git'
                echo 'hello world :)'
            }
        }
    }
}
